version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        # Client-only for Amplify Hosting. Express is NOT hosted here.
        # Set Amplify env VITE_API_URL to your Node API host for generation.
        # Checkout uses Supabase Edge Functions (create/confirm Razorpay).
        - npm run build:web
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .npm/**/*
customRules:
  - source: '/<*>'
    target: '/index.html'
    status: '200'
